# Datasets directory

This folder contains CSV, JSON, or JSONL datasets representing historical customer support tickets, conversation logs, and Q&A pairs.

These files will be utilized for:
1. Fine-tuning language models to capture agent tone and voice.
2. Building prompt evaluation sets.
3. Training classifier models to automatically assign ticket categories.
